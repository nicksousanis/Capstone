@workouts.map do |workout| {
  id: workout.id,
  user_id: workout.user_id,
  distance: workout.distance,
  # workout_time: "#{workout.workout_time.hour} hour(s) #{workout.workout_time.min} minute(s) #{workout.workout_time.sec} second(s)",
  workout_time: workout.workout_time,
} end
